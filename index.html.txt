<!DOCTYPE html>
<html lang="ar">
<head>
<meta charset="UTF-8">
<title>إلى ماريز ❤️</title>

<style>
@import url('https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap');

body {
    margin: 0;
    font-family: 'Cairo', sans-serif;
    background: linear-gradient(135deg, #ffe6eb, #fff);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    overflow: hidden;
}

.card {
    background: white;
    padding: 25px;
    border-radius: 25px;
    box-shadow: 0 15px 35px rgba(255, 77, 77, 0.3);
    text-align: center;
    max-width: 360px;
    animation: pop 0.8s ease;
}

@keyframes pop {
    from { transform: scale(0.8); opacity: 0; }
    to { transform: scale(1); opacity: 1; }
}

input {
    padding: 12px;
    border-radius: 12px;
    border: 2px solid #ff4d4d;
    font-size: 18px;
    text-align: center;
}

button {
    margin-top: 15px;
    padding: 10px 30px;
    background: #ff4d4d;
    border: none;
    color: white;
    border-radius: 12px;
    font-size: 18px;
    cursor: pointer;
}

#content { display: none; }

img {
    width: 100%;
    border-radius: 20px;
    margin: 15px 0;
}

video {
    width: 100%;
    border-radius: 20px;
    margin-top: 15px;
}

h1 { color: #ff4d4d; }

p { font-size: 18px; color: #444; }

.heart {
    position: fixed;
    top: -5vh;
    font-size: 22px;
    animation: fall linear forwards;
}

@keyframes fall {
    to { transform: translateY(110vh); }
}
</style>
</head>

<body>

<div class="card" id="lock">
    <h2>أدخلي كلمة السر يا ماريز ❤️</h2>
    <input type="password" id="pass" placeholder="•••">
    <br>
    <button onclick="unlock()">دخول</button>
</div>

<div class="card" id="content">
    <h1>ماريز ❤️</h1>
    <p><strong>من يوم 20 / 8 / 2025</strong></p>

    <img src="love.jpg" alt="Love">

    <p id="msg"></p>

    <button onclick="showSurprise()">Surprise 🎁</button>

    <div id="surprise" style="display:none">
        <video controls>
            <source src="love.mp4" type="video/mp4">
        </video>
    </div>
</div>

<script>
const text = "من أول يوم جمعني بيكي وأنا حسيت إن الدنيا اختارتلي قلبي صح. إنتِ الأمان، وإنتِ الضحكة، وإنتِ أجمل صدفة في حياتي. كل لحظة معاكِ بحبك فيها أكتر، ووعد مني أفضل جنبك دايمًا يا ماريز ❤️";
let i = 0;

function unlock() {
    let p = document.getElementById("pass").value;
    if (p === "208") {
        document.getElementById("lock").style.display = "none";
        document.getElementById("content").style.display = "block";
        typeText();
        setInterval(createHeart, 300);
    } else {
        alert("كلمة السر غلط يا جميلة 😌");
    }
}

function typeText() {
    if (i < text.length) {
        document.getElementById("msg").innerHTML += text.charAt(i);
        i++;
        setTimeout(typeText, 45);
    }
}

function showSurprise() {
    document.getElementById("surprise").style.display = "block";
}

function createHeart() {
    const heart = document.createElement("div");
    heart.className = "heart";
    heart.innerHTML = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.animationDuration = (Math.random() * 3 + 4) + "s";
    document.body.appendChild(heart);
    setTimeout(() => heart.remove(), 6000);
}
</script>

</body>
</html>